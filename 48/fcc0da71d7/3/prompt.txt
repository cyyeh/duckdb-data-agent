for updating memory, agent should check if it need to update original memory, not generate a new one that conflicts with old one

---

I mean in this duckd-data-agent

---

for recall_memories in thinking block, I don't see data inside, is that correct?

---

I think all tool results should be rendered in thinking block

---

make sure all input/output of tools results also saved in sqlite, so I could see them when switch back to original conversation

---

create new branch and commit and push and raise pr and merge