apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

patches:
  - path: patches/opensandbox-runtime.yaml

configMapGenerator:
  - name: opensandbox-config
    files:
      - sandbox.toml=config.docker.toml

generatorOptions:
  disableNameSuffixHash: true
