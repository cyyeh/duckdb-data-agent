create fake excel, json, parquet file for me to load into duckdb

---

create two csv files exceeding 500 MB in total

---

use filename(including suffix) as names in table list

---

fix the style of table list that if table name is too long, make it abbreviated, not spreading to multiple lines, and hover over to show full table name

---

commit this