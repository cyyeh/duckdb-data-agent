apiVersion: v1
kind: Service
metadata:
  name: opensandbox
  labels:
    app: opensandbox
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
  selector:
    app: opensandbox
