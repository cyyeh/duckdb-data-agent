don't need to put tests folder in docker image

---

also poetry install doesn't need to install dev dependencies in dockerfile right?

---

update readme to reflect new functionality: per user duckdb session

---

commit this

---

fix 
public
Fast refresh only works when a file only exports components. Move your React context(s) to a separate file. in SessionContext.tsx

---

shouldn't you put content of sessionContext.ts to hooks/useSessionId.ts?